---
scope: Handy Tools full-suite architecture
purpose: build-substrate
altitude: feature
topic: Handy Tools Architecture Spine
updated: 2026-07-31T18:34
---

- (decision) AD-1: zero runtime third-party libraries. ADOPTED from PRD §1 + UX §1.1. Vendored libs allowed only under assets/js/vendor/ as static files.
- (decision) AD-2: Tool Contract is the unit of inclusion. ready=false Tools don't appear in home/palette/packs/search/embed. CI gates. ADOPTED PRD §4.1.
- (decision) AD-3: Site Data (tools.json) is the single source of truth for discovery. CI validates schema and slug resolution. ADOPTED PRD §4.2.
- (decision) AD-4: Shell owns global concerns, Tools own local. Tools call HT.hist/HT.toast/HT.t only; never re-implement. ADOPTED PRD §4.3+4.5 + UX §1.1.
- (decision) AD-5: URL is canonical state. Shell uses query (?tool/?pack/?embed/?dashboard/?quality/?privacy); Tool uses fragment (#k=v). Delegated to HT.url. ADOPTED PRD §4.1c4+§4.4.
- (decision) AD-6: localStorage is namespaced via storage-registry.js. Two namespaces ht.* (runtime) and handy-tools.* (user data). /privacy generated from the registry. ht.theme grandfathered.
- (decision) AD-7: Embed mode is a Shell flag, not a separate app. Shell hides chrome, locks theme/locale; Tool renders identically. postMessage implemented once in HT.embed. ADOPTED PRD §4.4.
- (decision) AD-8: SW caches Shell (precache) and per-Tool (SWR runtime cache, LRU evict at 8MB or 50 Tools). New Shell version invalidates precache. ASSUMPTION on budget.
- (decision) AD-9: Cross-Tool interaction only through Site Data + Shell via HT.actions.dispatch. No Tool-to-Tool imports. Pack pages are the only grouping surface. ADOPTED PRD §4.2+§4.8.
- (decision) AD-10: i18n uses Intl.* APIs only; no locale data shipped. RTL-safe via CSS logical properties. ADOPTED PRD §4.7.
- (decision) AD-11: Trust surface (/privacy, /quality) is generated, not authored. /privacy from storage-registry, /quality from tools.json, view-source from site-config. ADOPTED PRD §4.6.
- (decision) AD-12: No SSR, no backend, no build step. Static files only. ES2018 baseline. Vendored libs require ES source beside any min. ADOPTED from PRD §1 + brownfield (no package.json, no dist/, no node_modules/).
- (decision) AD-13: Dependency direction is one-way Shell→Tool. Tool calls HT.* only; never imports/reaches into Shell. Shell loads Tools on demand via dynamic <script> insertion (AD-12 forbids import()).
- (assumption) Travelling the brownfield: 33 tools under tools/<slug>/, shared assets/js/{utils,layout,theme}.js + assets/css/{base,components,tools}.css, qrcode.js vendored. No package.json, no dist/, no node_modules/. ht.theme key already in use.
- (assumption) ASSUMPTIONs in spine: SW budget=8MB or 50 Tools; ht.theme grandfathered; static host=GitHub Pages; QR encoder MIT-vendored kept; pack-assets dir speculative; tokens hand-authored (no generator yet); message catalogs hand-edited (no Crowdin). All tagged [ASSUMPTION] inline.
- (question) Open: GitHub Pages is the host but AD-12 says 'any static host' — pin a specific deployment story in a follow-up. Routable URL strategy for /embed/<slug> vs /?embed=<slug> — PRD §4.4 uses both; spine honors both but a single canonical is cleaner.
- (change) AD-5 tightened: codec grammar added (UTF-8 percent, flat k/v, sort-order canonical, defaults omitted, schema declared in tools.json, _v for version, pack-defaults precedence).
- (change) AD-6 tightened: each registered key has exactly one owner module; Tools own only their own history keys; shared keys accessed via HT.* APIs.
- (change) AD-7 tightened: postMessage protocol specified (instance-scoped UUID, envelope v=1, allowlist, 64KB size cap, origin checks, teardown).
- (change) AD-8 tightened: CACHE_VERSION atomic, mirrored in tools.json (releaseVersion), CI rejects divergence; skipWaiting + clients.claim; navigation routes enumerated; beforeinstallprompt called out as Chromium-only.
- (change) AD-14 added (Shell Public API Contract): every HT.* method has signature, owner, stability level (stable/experimental/internal); bypass prohibited; Tool-provided APIs via HT.provide().
- (change) AD-15 added (Brownfield migration): docs/tool-inventory.md classifies each existing tool as legacy/candidate/ready; home grid migrates only after every legacy tool is ready; CI enforces inventory.
- (change) Stack: ES5 baseline corrected from ES2018; ES2018 features permitted in new code, existing utils.js/layout.js/theme.js marked [TO MIGRATE]. GitHub Pages target host phrased as default + 're-verify at deploy time'. forced-colors reclassified as UA mode. prefers-reduced-motion marked [TO IMPLEMENT].
- (change) Structural Seed: added 'Current state vs target state' table enumerating every [TO CREATE] vs [EXISTS] file (15 to create, 7 existing). Makes greenfield scope explicit. addresses both reviewers' 'no manifest/SW/tools.json' findings.
- (event) spine drafted; 15 ADs; reviewer gate applied (both reviewers returned); 6 high-severity fixes applied (AD-5 codec, AD-6 ownership, AD-7 embed protocol, AD-8 SW versioning, AD-14 API contract, AD-15 brownfield migration); status=draft, awaiting user confirm to flip final
- (event) spine finalized; status=final; 15 ADs locked; lint passing; ready for bmad-create-epics-and-stories
